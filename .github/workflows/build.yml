name: build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      #- uses: docker/login-action@v3.1.0
      #  with:
      #    registry: ghcr.io 
      #    username: ${{ github.actor }}
      #    password: ${{ secrets.GITHUB_TOKEN }}
      #- run: docker build -t vite/host:clean . --build-arg NPM_TOKEN=${{ secrets.PAT }}
      #- uses: docker/build-push-action@v5.3.0
        #with:
          #tags: deuspaul/calculator:latest
          #build-args: |
            #NPM_TOKEN=${{ secrets.PAT }}
      
    
